# Linux arm64 evidence

**Status**: Deferred — no supported Linux arm64 host is available in this implementation session.

Run the standard active-path/version and second-apply checks on at least one supported glibc arm64
server. Alpine arm64 remains separately deferred until its exact Helix package and all musl artifacts
are verified. Record `uname -m`, `/etc/os-release`, normalized target, active command paths, versions,
and both apply logs.
